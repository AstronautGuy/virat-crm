---
wave: 1
depends_on: ["22-01-PLAN.md"]
files_modified:
  - src/server/api/routers/reports.ts
  - src/server/api/root.ts
  - src/server/api/routers/crm.ts
autonomous: true
---

# Plan 22: Daily Reports & Order Balance - Wave 1

## Tasks

### 22-01-01: Create Reports Router

<read_first>

- src/server/api/routers/crm.ts
- src/server/db/schema/daily_reports.ts
  </read_first>
  <action>
  Create `src/server/api/routers/reports.ts` with:
- `submitReport`: `protectedProcedure` taking `content: z.string()` and `customerId: z.string().uuid().optional()`.
  - Automatically sets `userId` and `branchId` from context.
- `listMyReports`: `protectedProcedure` for employees to see their own history.
- `listBranchReports`: `managerProcedure` for viewing all reports in the branch (filtered by `branchId`).
- Use `eq(dailyReports.branchId, ctx.user.branchId)` for isolation.
  </action>
  <acceptance_criteria>
- File exists: `src/server/api/routers/reports.ts`
- Router uses `protectedProcedure` and `managerProcedure`.
  </acceptance_criteria>

### 22-01-02: Register Router

<read_first>

- src/server/api/root.ts
  </read_first>
  <action>
  Register `reportsRouter` as `report` in `src/server/api/root.ts`.
  </action>
  <acceptance_criteria>
- `src/server/api/root.ts` contains `report: reportsRouter`.
  </acceptance_criteria>

### 22-01-03: Update CRM Router with Financial Stats

<read_first>

- src/server/api/routers/crm.ts
  </read_first>
  <action>
  Update `getCustomer` in `crmRouter` to include a summary of:
- `totalInvoiced`: Sum of `sales.invoiceAmount`
- `totalPaid`: Sum of `sales.receivedAmount`
- `totalPending`: Sum of `sales.balanceAmount`
  Use Drizzle `sum` or just aggregate in JS if record count is small.
  </action>
  <acceptance_criteria>
- `getCustomer` response includes `financials` object with aggregated totals.
  </acceptance_criteria>

---

_Plan: 22-02-api_
_Wave: 1_
