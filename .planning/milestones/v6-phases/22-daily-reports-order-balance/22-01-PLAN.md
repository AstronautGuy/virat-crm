---
wave: 0
depends_on: []
files_modified:
  - src/server/db/schema/daily_reports.ts
  - src/server/db/schema/index.ts
  - src/server/db/schema/sales.ts
autonomous: true
---

# Plan 22: Daily Reports & Order Balance - Wave 0

<threat_model>

- **T-22-01**: Unauthorized access to reports from other branches. (Mitigation: strict branchId filtering in SQL/ORM).
- **T-22-02**: Manipulation of `balanceAmount` via client-side calculations. (Mitigation: calculate balance on server-side only).
  </threat_model>

<schema_push_requirement>
**[BLOCKING] Schema Push Required**

- ORM detected: Drizzle
- Push command: `npx drizzle-kit push`
- This task is mandatory before moving to Wave 1.
  </schema_push_requirement>

## Tasks

### 22-00-01: Create Daily Reports Schema

<read_first>

- src/server/db/schema/customers.ts
  </read_first>
  <action>
  Create `src/server/db/schema/daily_reports.ts` with:
- `id`: serial primary key
- `userId`: uuid references users.id (not null)
- `branchId`: integer references branches.id (not null)
- `reportDate`: timestamp with timezone (defaultNow)
- `content`: text (not null)
- `customerId`: uuid references customers.id (optional)
- Index on `userId`, `branchId`, and `reportDate`.
  </action>
  <acceptance_criteria>
- File exists: `src/server/db/schema/daily_reports.ts`
- Table name is `virat-crm_daily_report` (using pgTableCreator prefix).
  </acceptance_criteria>

### 22-00-02: Export Schema

<read_first>

- src/server/db/schema/index.ts
  </read_first>
  <action>
  Add `export * from "./daily_reports";` to `src/server/db/schema/index.ts`.
  </action>
  <acceptance_criteria>
- `src/server/db/schema/index.ts` contains the export.
  </acceptance_criteria>

### 22-00-03: [BLOCKING] Push Schema to DB

<action>
Run `npx drizzle-kit push` to apply the new schema.
</action>
<acceptance_criteria>
- Command exits with 0.
</acceptance_criteria>

---

_Plan: 22-01-infrastructure_
_Wave: 0_
