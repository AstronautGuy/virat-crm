# Concerns

- **Boilerplate:** The application is a boilerplate generated by `create-t3-app`.
- **Cleanup Needed:** Needs actual business logic and cleanup of default "hello world" code, like the placeholder `posts` table and `hello` tRPC procedure.
